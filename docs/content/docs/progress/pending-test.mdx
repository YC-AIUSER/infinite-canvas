---
title: 待测试
description: 当前版本已实现但仍需人工验证的变更项
---

# 待测试

- 画布 Agent 精简快照：`canvas_get_state`、批量操作及其他画布工具默认仅返回必要结构，长正文和 Toonflow 当前产物截断到 200 字，历史版本、洗词明细和内联 dataURL 不再进入默认结果；`canvas_get_state` 可传 `nodeIds` 按需取回指定节点的完整未截断内容。需在真实大画布连续对话中确认 rollout 增长速度与工具读取体验。
- Toonflow 高潮/爆点镜三件套：分镜表、镜头合同、动作合同提示词会把镜头身份、L5 五部位参数、Speed Ramp 四要素与 L4/L5 景别写入固定字段；分镜表质量检查现已读取动作合同，删除全局表演档位判据，字段无信息时显示「需人工确认」，只有弱词、镜级低档位、残缺 Speed Ramp 或 L0-L3 景别等明确反证才给 warning。需用真实模型生成冲突爆点、变身或冲锋段，确认三个环节都按约定字段产出，质量检查能识别齐全、待确认与明确不符合三种状态。
- Toonflow 节点流式回显：文本类环节（剧本/创意/镜头合同等）生成中在节点内实时逐段显示模型输出，结构化环节（分镜表/分镜决策锁定表/跨段继承表/资产库）显示小字灰底的原始流尾部并标注「完成后转为表格」，落定后自动换回正式产物渲染。生成期间节点自动撑高到 440px（模板 190px 只够挤出一行等于看不见），原高度记进 `streamRestoreHeight`，收尾/刷新时原样还原；用户自己已调得更高的节点不动尺寸。已用本地假 SSE 服务端到端实测剧本（文本模式）与分镜表（raw 模式）：文本随流增长并自动滚动到底、节点撑开后可读多行、成功与失败落定后流式区消失且高度与字段全部复原，刷新中断路径经 `hydrateToonflowProject` 清理后同样回到原高度。**仍需真实模型实测**：视频工作台 Module4 合成的流式（与剧本同一条链路但未单独跑过）、Gemini 调用格式下的流式分片是否同样逐段到达、长剧本连续流式时画布的滚动与性能表现。已知取舍：撑高期间节点可能与下方一行节点短暂重叠，生成结束即恢复。另：生成中的取消入口在 Toonflow 节点卡片上不可达（「生成中」按钮为禁用态），取消路径的清理逻辑未经 UI 验证。
- Toonflow Module4 两步生成（第三块批A）：视频工作台「生成」改为先合成可审的 Module4 六段文本（参考图索引/故事线/Tone/BGM衔接/风格/画面要求），节点产物区显示全文，审毕点「确认并生成视频」（带费用确认）才调 cano。需真实合成实测：故事线是否合段自然叙事且无逐镜列表/Shot 编号、左右站位是否逐句写死、首句是否覆盖钩子、校验器违规带反馈自动重试是否生效、BGM衔接是否 ≤200 字。
- Toonflow 人声抑制与口型（D8 台词剥离，第三块批A）：视频 prompt 画面要求段硬编码「只生成环境音效，禁人声/对白/旁白音频；台词文本仅控口型」。cano 无 `generate_audio` 开关，**抑制是否有效必须真实出片验证**（设计文档风险表首项）；同时验证 dialogue 型台词该角色口型正确、OS 型全段全员闭口。无效时的回退方案：成片以配音轨压制 + 记录，或改渠道再议。
- Toonflow P7 七项质检单与最小返修闭环（第三块批B）：视频工作台段实例出片后需逐项勾选七项检查（身份/资产/摄影/动作/叙事节奏/声音字幕/技术质量），可记 P0/P1/P2 与备注；存在未清 P0 或未全勾时「通过」按钮禁用并显示原因（数据层同样拦截，Agent 绕不过）。P0/P1 会按五级最小返修顺序生成可编辑计划，每条包含原因、输入锚点、保留内容、替换范围和验收标准；P2 仅列为建议优化。预计重生成镜头超过本段 20% 时显示成本确认警示，并固定提示同一问题连续两次失败后改镜头设计或剪辑规避、保留原始素材与版本。需实测质检勾选、返修字段编辑持久化、P0 拦截与解除、20% 边界及确认提示；本期不执行单镜级重生成。
- Toonflow 接缝检查升级（第三块批B）：边界卡展示对应 directing-lock 五行缝合同（未签提示回锁定表补签）、上段尾帧/本段首帧抽帧对照（走本地 canvas-agent ffmpeg，未连接或失败时降级只显示合同文本、勾选不受阻）、按合同景别/动机行高亮十条剪辑手法建议。需实测两段已出片场景的抽帧对照与降级路径。
- Toonflow 音频混音激活（第三块批B）：从本段分镜表提取出口对白/OS 逐句 TTS（角色音色按节点 voiceMap 配置，OS 用旁白音色兜底），按段内镜头时长累计计划偏移；全部成功进 review 人工试听后通过；分镜表新版本会使配音失效。需真实 TTS 实测：音色映射、逐句音频可播、计划偏移与实际视频时长的偏差幅度（超限再立对齐工具项）。
- Toonflow 成片导出混音与响度（第三块批B）：拼接弹窗新增「混入配音轨」（需全段音频混音已通过；跨段偏移用 ffprobe 实测时长）与「响度归一 -16 LUFS」两个默认关的选项，任一勾选走转码，不勾时保持原无损拼接。需实测：混音成片人声位置对齐度、响度归一后整片响度、不勾选项时产物与旧版一致。
- Toonflow 缝合同注入（第三块批A）：directing-lock.seams[] 首次被视频层消费——入缝段故事线首句受「接同一动作后半、禁止重新建立空间」约束，出缝段末句收动作中间态且画面要求段带「段尾 0.5s 不起新持续音」英文句。需相邻两段连拍实测：缝两侧是否可硬切拼接、音频边界是否真被模型遵守。
- 全站 Agent：新增 `generation_get_status` 工具，画布生成节点可按 `nodeIds` 查询，生图和视频工作台提交后会返回 `taskId` 并可查询排队、运行、成功或失败状态；需验证查询只由当前活动标签页返回。
- 本地 Agent 多标签页隔离：同时打开两个不同画布并连接同一个 Agent，分别聚焦标签页后通过 MCP 读取和修改画布，操作应只落在当前聚焦页面；网页面板发起的整个 Codex turn 应固定操作发起页面，即使中途聚焦另一标签页也不能切换目标；关闭当前页面后应回退到最近聚焦且仍连接的页面，其他页面回传同一请求结果应被拒绝。
- 本地 Agent 多标签页会话同步：所有标签页共享同一个站点级 Codex 活跃线程；任一页面发送消息、新建、恢复或删除会话后，其他页面应同步活跃线程和聊天记录；Agent 输出仅显示在事件所属线程，运行中不能新建、恢复、删除或再次发送任务。
- 本地 Agent 运行状态同步：在一个标签页运行较长 Codex 任务，等待某张工具卡显示「工具完成」后再打开或刷新第二个标签页；第二个标签页应立即显示 Codex 正在运行并禁用发送，整轮结束后两个标签页同时恢复；工具卡只显示「工具完成」，整轮结束由「本轮完成」表示。
- 画布节点创建：鼠标左键双击画布空白区域会在点击位置打开节点选择菜单，可创建文本、图片、视频、音频、生成配置和组节点；双击已有节点或连线不应触发菜单。
- Toonflow 质感样板：资产卡可生成 16:9 横版 2×3 六格无人物材质微距，留空取材描述时使用默认织物/扣具/皮革/鞋底/土石/帆布，填写后替换取材；视频参考中样板排在最后，只提供表面纹理、磨损、颗粒感与色调，布光读取分镜决策锁定表的结构化主策略。需实测样板不出现人物、天空、光束或环境空镜，成片保留质感且不被样板改变光位、光比和光线方向；禁止上传照片级真人质感的成片抽帧作参考。
- cano 视频生成幂等保护：创建任务携带唯一 `Idempotency-Key`，同一次传输层重试复用 Key，`CONTENT_BLOCKED` 终态重新提交换新 Key；需验证网络抖动不重复建任务扣费，409 明确提示幂等键冲突。（2026-07-22 实测：cano 建任务回传的 `multipart` 并非 `true`、`refAudioCount` 不回传，故这两个回显字段只 `console.warn` 记录、不作阻断——否则会把已建成、已计费的任务判失败丢掉。）
- Toonflow blockout 故事板渲染粒度（2026-07-22 实测出图观察）：段级故事板已真实出图，方向正确（哑光中性灰、无五官毛发、装备只有概括体积、三层纵深清楚），但渲染粒度偏精细——躯干出现服装分片、四肢关节分段、手指分节，更接近 skill 的「精细白模」而非「blockout 粗模」。本轮未翻车（装备无增殖），但出现装备数量错时应首先怀疑此处。若需收紧，在 `buildStoryboardPagePrompt` 的禁制清单补「手指分节 / 服装分片 / 关节缝」。
- Toonflow Module3 故事板：生成改为不传资产卡或色板参考图的 blockout 未贴图中性灰模，只锁机位、景别、人物左右站位、体块、姿态和纵深遮挡；视频生成改由第 1 张故事板锁构图、后续角色/场景/道具/色板卡锁外观与颜色，且不再引用首帧组，需实测灰模无五官装备细节、成片角色装备服装与色板稳定、左右站位不漂移。
- Toonflow 人物资产卡：角色卡生成提示词改为 16:9 横版人物设定页，需验证正面、侧面、背面、面部特写、关键状态、装备细节和底部中文总述均能稳定生成，且面部特写能展示角色识别特征。
- cano 视频生成：任务被随机内容审核以 `CONTENT_BLOCKED` 拦截时会自动重新提交最多 2 次，间隔从 3 秒起；需验证重试期间不会提前报错，全部失败后仍展示 cano 原文与可再次尝试提示，`VALIDATION_ERROR` 等其他错误不重试。
- Codex App 插件：修复插件通过 `mode=new/recent/choose` 打开画布后未自动连接本地 Agent 的问题，并将端口冲突回退命令从 Next.js 更新为 Vite；需验证新建、最近和选择画布三种模式都能自动连接。
- 本地 Agent 连接说明：新增提醒说明只有安装 Codex 插件或手动添加 MCP 后才会增加 Codex token 消耗，直接运行 `npx -y @yinchenhuang/canvas-agent` 不会安装 MCP；需验证文案位置清晰。
- 本地 Agent 连接说明：画布面板和配置页 Codex Tab 改为区分“Codex 插件启动画布”和“直接运行 Agent 后网页连接”两种方式，需验证文案和布局清晰。
- 全站 Agent：Agent 面板从画布页抽离为全站常驻面板，挂在全局布局右侧，开/关只挤压左侧内容不遮挡；顶栏和画布工具条各有一个开关按钮，需验证在首页、生图、视频、素材、提示词库、画布等页面都能打开面板并保持 SSE 连接不断开。
- 全站 Agent：新增 `site_navigate` 工具，Agent 可直接跳转 `/`、`/canvas`、`/canvas/:id`、`/image`、`/video`、`/prompts`、`/assets`、`/config`；导航直接执行不走工具确认，需验证跳转正确且面板不消失。
- 全站 Agent：画布工具（canvas_apply_ops 等）仅在当前处于画布页时可用，不在画布页调用会返回“当前不在画布页，请先用 site_navigate 打开画布”，需验证 Agent 会先导航到画布再执行画布操作。
- 全站 Agent：Codex 会话改为单一站点级连续线程（不再按 canvasId 分），跨页面/跨画布保持同一条对话上下文，历史记录即这条线程；本地 Agent workspace 从按画布分改为固定 `site` workspace（`~/.infinite-canvas/codex-workspaces/site`），已装 `@yinchenhuang/canvas-agent` 的用户需更新到新版本才生效。
- 全站 Agent：移除仅前端调用 OpenAI responses 接口的在线画布助手死代码（含 `requestToolResponse`），Agent 统一走 MCP + 本地 Codex 链路。
- 画布节点名称：每个节点保留 `title` 名称字段，默认名称显示在节点上方，双击名称可直接编辑，节点信息弹窗和 JSON 可查看该字段。
- 画布节点工具条：节点顶部工具条改为点击选中节点后显示，单纯鼠标移入 / 移出节点不应再频繁弹出。
- 画布组节点：组内子节点优先响应点击和拖拽；当鼠标位于组内节点上时，应移动 / 编辑该节点，而不是移动整个组。
- 画布组节点：工具栏新增“组”节点，普通节点拖入组区域会高亮目标组，松手后自动吸附进组；拖出组区域会解除归属，拖动组节点会一起移动组内子节点。
- GitHub Pages：新增版本 tag 触发的前端静态站点自动发布 workflow，需在仓库 Pages 设置中选择 GitHub Actions 并验证路由刷新回退。
- 画布图片切图：行数 / 列数会生成可直接拖拽的等分切图线，并支持新增横向 / 纵向线、删除单条线、重置切图线和切片数量预览。
- Toonflow 专用画布：画布库新增 Toonflow 生产画布入口，按三段布局自动创建项目/剧集、剧本、资产库、空间合同、分镜表、镜头合同、动作合同、故事板页、分镜图/首帧、过审、视频工作台、接缝检查、音频混音、成片/导出独立节点；Toonflow 模式保留与普通画布一致的通用创建入口（文本/图片/视频/音频/配置/组/上传/素材及拖线建节点），需人工确认节点布局、连线和刷新持久化效果。
- Codex App 插件文档：新增插件安装教程，区分 AI 自动安装、手动安装和本仓库开发调试安装；同步优化插件 README 与技能说明。
- 本地 Codex 连接画布文档：新增 Canvas Agent、MCP、SSE 与浏览器画布交互原理说明，需确认文档导航和描述是否清晰。
- 顶部导航：缩小桌面顶部导航栏高度，导航项下划线和内容垂直对齐需确认。
- 配置与用户偏好：新增独立顶部导航页面，并在页面和弹窗中复用同一套配置面板；新增 Codex Tab，展示插件安装和本地 Agent 启动步骤，可配置本地 Agent 地址、Connect token 和工具执行确认开关。
- Codex 全局连接：配置页可直接连接本地 Canvas Agent，连接状态同步到画布和顶部 OpenAI 状态图标，刷新后会按本地保存的地址和 token 自动尝试重连；需验证已连接、连接中、连接失败、断开和刷新自动重连状态。
- 画布 Agent：Codex 旧 thread 在重启后不可用时会自动新建会话并重试，需验证进入画布直接发送消息不会再出现 `thread not loaded` 或 `no rollout found`。
- 画布 Agent：右侧 Agent 面板移除网站 / 本机模式切换，固定使用本机 Agent。
- 配置与用户偏好：每个模型渠道新增 OpenAI / Gemini 调用格式选择，默认 OpenAI，切换默认格式时同步更新默认 Base URL；渠道 Tab 增加紧凑醒目的提醒，说明模型是否可选需要到“模型”Tab 选择。
- Gemini 调用格式：支持拉取 Gemini 模型列表，并用于文本问答、在线 Agent 工具调用、基础生图和图生图请求。
- Gemini 生图：生图工作台和画布生图会把尺寸配置转换为 Gemini `responseFormat.image.aspectRatio` 传入，需验证 1:1、9:16、16:9 及自定义像素尺寸的输出比例。
- 音频和视频生成：选择 Gemini 调用格式时先给出不支持提示，仍需使用 OpenAI 兼容渠道。
- WebDAV 同步：移除 Next.js 转发代理和连接方式选择，改为浏览器前端直接连接 WebDAV 服务。
- 提示词库：移除 Next.js route 抓取和服务端内存缓存，改为浏览器前端直连 GitHub 原始文件并缓存到 IndexedDB。
- 提示词库：移除已 404 的 `EvoLinkAI/awesome-gpt-image-2-API-and-Prompts` 来源。
- 前端构建：移除 Next.js，改为 Vite + React Router，并将前端目录调整为 `pages` / `components` / `stores` / `lib` / `layouts` / `styles`，需验证首页、图片/视频工作台、素材、提示词库和画布路由跳转。
- 前端构建：修复 TypeScript 严格检查下的构建报错。
- Vercel 部署：新增 `web` 和 `docs` 两个子项目的 Vercel 配置，需分别验证静态前端和文档站部署。
- Docker 部署：运行镜像从 Node serve 改为 nginx 静态托管，需验证前端路由刷新回退。
- 环境变量：移除过期的后端 `.env.example` 示例文件。
- 生图工作台：修复失败图片点击重试后只保存在页面状态、刷新或跳转后丢失的问题，重试成功结果会写入本地图片存储和生成记录。
- 视频生成：任务查询接口直接返回 `url` / `result_url` / `video_url` 时会跳过 `/content` 下载，并优先保存到浏览器本地媒体存储。
- 视频生成：任务创建或查询失败时会解析接口返回的 `msg` / `message` / 嵌套 JSON 错误，并在生成结果失败卡片里展示真实原因。
- 画布生成配置：修复切换文本/视频/音频模式时模型选择仍显示为生图模型（gpt-image-2）的问题，切换模式后应自动匹配对应能力的默认模型。
